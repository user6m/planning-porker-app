{
	// プロジェクト参照のルート。実際の設定は用途ごとの tsconfig.*.json にある。
	// - tsconfig.app.json:    src/client (ブラウザ / React) + src/shared
	// - tsconfig.worker.json: src/worker (Cloudflare Workers) + src/shared + test
	// - tsconfig.node.json:   vite.config.ts (Node.js)
	"files": [],
	"references": [
		{ "path": "./tsconfig.app.json" },
		{ "path": "./tsconfig.worker.json" },
		{ "path": "./tsconfig.node.json" }
	]
}
